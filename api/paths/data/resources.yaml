get:
  operationId: getResources
  tags:
    - Data
  summary: Get Resources Data
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            required:
              - data
            properties:
              data:
                type: array
                items: 
                  $ref: ../../components/schemas/ResourceData.yaml
  security: []