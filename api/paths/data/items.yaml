get:
  operationId: getItems
  tags:
    - Data
  summary: Get Items Data
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            required:
              - data
            properties:
              data:
                type: array
                items: 
                  $ref: ../../components/schemas/ItemData.yaml
  security: []