post:
  operationId: transferResources
  tags:
    - Inventories
  summary: Transfer resources
  description: Transfer resources from one inventory to another
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/TransferResourcesRequest.yaml"
  responses:
    '204':
      description: 'Successfully transferred resources'
    '401':
      $ref: "../../components/responses/InvalidAgentToken.yaml"
    '403':
      $ref: "../../components/responses/AccessDenied.yaml"
    '422':
      description: 'Invalid/unsupported transfer direction'
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ErrorResponse.yaml"
  security:
    - AgentAuth: []