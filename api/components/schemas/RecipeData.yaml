type: object
required:
  - id
  - required_facility
  - min_tier
  - duration
  - inputs
  - outputs
properties: 
  id:
    type: string
  required_facility:
    type: string
    description: facility type
    example: smelter
  min_tier:
    type: integer
  duration:
    type: integer
    description: In seconds
  inputs:
    type: array
    items: 
      $ref: RecipeInput.yaml
  outputs:
    type: array
    items: 
      $ref: RecipeOutput.yaml