type: object
required:
  - id
  - type
properties:
  id:
    type: integer
    description: Waypoint's ID inside system
  type:
    type: string
    description: Waypoint's type
    enum:
      - STATION
      - ASTEROID